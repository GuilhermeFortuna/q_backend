#!/usr/bin/env bash
set -euo pipefail

echo "==> Running pre-commit hook (ruff check & black check)..."
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"

uv run ruff check .
uv run black --check .
